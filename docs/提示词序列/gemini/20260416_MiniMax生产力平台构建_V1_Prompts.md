# MiniMax AI 生产力平台构建提示词序列 (V1)

> **元数据信息**
> - **生成模型**: Gemini 3 Flash
> - **生成时间**: 2026-04-16 19:53:51
> - **参考方案**: [[20260416_MiniMax生产力平台方案_V1.md]]
> - **设计目标**: 将生产力平台构想拆解为可落地的、分阶段的 AI 执行指令。

---

## 阶段一：打地基 (基础架构与用户隔离)
**提示词目标**: 建立多用户环境和数据隔离逻辑。

> **AI 提示词：**
> “帮我把这个项目的地基打好。现在只有个单页面，我要把它改成正式的生产力平台。要求：
> 1. 加入用户系统，用 SQLite 做持久化，实现注册和登录。
> 2. 引入‘项目 (Project)’的概念，用户所有的操作都必须归属于某个项目。
> 3. **核心要求**：实现严格的数据隔离。A 用户只能看到和管理自己的项目数据，B 用户同理。
> 4. 采用 Flask (或 FastAPI) 的多页面结构，把代码模块化，方便我后面往里加功能。”

---

## 阶段二：脚本工坊 (结构化创意写作)
**提示词目标**: 利用 MiniMax 文本模型实现结构化的短剧创作。

> **AI 提示词：**
> “在现有框架基础上，帮我搞个‘创意写作’模块。
> 1. 专门对接 MiniMax 的文本 API，用于生成短剧脚本。
> 2. **流程化设计**：支持‘生成大纲’ -> ‘人设分析’ -> ‘分集细化’的步骤。
> 3. 把生成的每一集脚本都存到数据库里，并关联到当前项目下。
> 4. 界面要简洁好用，能让用户手动微调生成的文本，然后再进行下一步。”

---

## 阶段三：视觉工坊 (分镜与素材生成)
**提示词目标**: 集成 MiniMax 图文模型，产出视觉资产。

> **AI 提示词：**
> “给项目加上‘视觉工坊’功能，调用 MiniMax 的文生图 (T2I) 和图生图 (I2I) 接口。
> 1. 用户可以在项目内基于脚本内容生成分镜插图。
> 2. 预设几个风格选项（比如：日漫风、电影写实、美漫插画）。
> 3. 生成的图片要自动重命名并存到后端存储，数据库里记录好路径，方便在项目详情里展示。”

---

## 阶段四：集成与监控 (看板与全家桶协同)
**提示词目标**: 完善配音功能及管理员监控。

> **AI 提示词：**
> “最后把剩下的活干完：
> 1. 加入‘配音实验室’，调用 MiniMax TTS 接口。能让用户给剧本里的角色选声音，一键把对白转成音频，关联到对应的剧本集数上。
> 2. 开发一个‘管理员看板’。管理员登录后能看到全站有多少人、消耗了多少 Token、API 余额还剩多少。
> 3. 优化整体 UI 导航，让用户在‘写剧本’、‘生图’、‘配音’之间流畅切换。”

---
*注：此提示词序列建议按顺序逐一投喂给 AI 助手执行，每步完成后验证无误再进行下一步。*
